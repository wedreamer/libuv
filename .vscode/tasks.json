{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "compile",
            "command": "/usr/bin/gcc",
            "args": [
                "-fdiagnostics-color=always",
                // "-I${workspaceFolder}/include",
                // "-L${workspaceFolder}/build",
                "-g",
                "${file}",
                "-o",
                "${workspaceFolder}/build/bin/${fileBasenameNoExtension}",
                "${workspaceFolder}/build/libuv.so",
                // "-ldl",
                // "-lrt",
                // "-pthread",
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "调试器生成的任务。"
        }
    ],
    "version": "2.0.0"
}